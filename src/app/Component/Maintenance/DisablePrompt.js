import { Typography } from "@mui/material"


const DisablePrompt = () => {
   return(
      <div>
         <Typography>Disable this item?</Typography>
      </div>
   )
}

export default DisablePrompt